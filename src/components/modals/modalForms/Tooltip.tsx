const Tooltip = () => {
  return (
    <div style={{ display: 'flex', flexDirection: 'column' }}>
      <button>edit</button>
      <button>delete</button>
    </div>
  );
};

export default Tooltip;
